# Core libraries
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
matplotlib==3.9.3

# Deep learning - Use TensorFlow 2.17 which works with protobuf 4.25.3
tensorflow==2.17.0
keras==3.5.0

# Computer vision
opencv-python==4.10.0.84

# Hand tracking and landmark detection
mediapipe==0.10.20

# Resolve protobuf conflicts — Use 4.25.3 for mediapipe compatibility
protobuf==4.25.3

# Utilities
joblib==1.4.2

# Notes:
# - TensorFlow 2.17 is used (same package supports GPU when CUDA/cuDNN are installed)
# - To use GPU on Windows, install the appropriate CUDA and cuDNN versions compatible with TensorFlow 2.17
#   See: https://www.tensorflow.org/install/source#gpu for compatibility matrix

